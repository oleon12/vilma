% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view.vilma.R
\name{view.vilma}
\alias{view.vilma}
\title{Unified interactive viewer for \emph{Vilma} spatial phylogenetic outputs}
\usage{
view.vilma(vilma)
}
\arguments{
\item{vilma}{A \verb{vilma.*} object — one of:
\code{vilma.dist}, \code{vilma.pd}, \code{vilma.beta}, or \code{vilma.null}.}
}
\value{
A \code{leaflet} HTML widget containing the appropriate interactive map.
If the object class is not recognized, an informative error is returned.
}
\description{
\code{view.vilma()} is a convenience wrapper that automatically detects the type of
\emph{Vilma} analysis result and opens the corresponding interactive visualization.
This function removes the need to remember individual viewer functions for
each result type, providing a streamlined and intuitive way to explore
biodiversity patterns across space.

It dispatches to the appropriate map viewer based on object class:\tabular{lll}{
   Class \tab Routed Viewer Function \tab Visualizes \cr
   \code{vilma.dist} \tab \code{view.vilma.dist()} \tab Species richness & abundance rasters \cr
   \code{vilma.pd} \tab \code{view.vilma.pd()} \tab Alpha–diversity rasters (PD, MPD, MNTD, PE, RaoQ, etc.) \cr
   \code{vilma.beta} \tab \code{view.vilma.beta()} \tab Beta–diversity rasters (UniFrac, PhyloSor, βMPD, βMNTD, Rao β, etc.) \cr
   \code{vilma.null} \tab \code{view.vilma.null()} \tab Null–model SES maps and significance patterns \cr
}


All viewers use \strong{leaflet} to render maps with:
\itemize{
\item Viridis color palettes
\item Pixel value pop-ups on cursor hover
\item Toggleable layers
\item Background basemap controls
}
}
\details{
This function is especially useful during exploratory phases, teaching,
and demonstration of phylogenetic spatial patterns, allowing users to
seamlessly jump between object types without manually specifying viewers.
}
\examples{
\dontrun{
# dist <- points_to_raster(occ_data)
# pd   <- faith.pd(tree, dist)
# beta <- unifrac(tree, dist)
# null <- mpd.calc.null(pd, tree, dist, iterations = 999)

view.vilma(dist)
view.vilma(pd)
view.vilma(beta)
view.vilma(null)
}

}
\seealso{
\code{view.vilma.dist}, \code{view.vilma.pd}, \code{view.vilma.beta}, \code{view.vilma.null}
}
\author{
Omar Daniel Leon-Alvarado (\url{https://leon-alvarado.weebly.com})
J. Angel Soto-Centeno (\url{https://www.mormoops.com})
}
\concept{Vilma visualization functions}
